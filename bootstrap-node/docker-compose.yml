services:
  bootstrap:
    build:
      context: ..
      dockerfile: bootstrap-node/Dockerfile
    ports:
      - "4001:4001"
    volumes:
      - bootstrap-data:/data
    environment:
      - PORT=4001
    restart: unless-stopped

volumes:
  bootstrap-data:
